<h1>Cats<h1>

<ul>
    <% @cats.each do |cat| %>
        
    <li> <%= link_to cat.name, cat_url(cat.id) %> </li>
    <% end %>
</ul>

<%= button_to 'Add a Cat', new_cat_url, method: :get %>

<%= button_to 'New Rental Request', new_cat_rental_request_url, method: :get %>
